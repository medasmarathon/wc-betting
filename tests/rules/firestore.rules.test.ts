import { describe, it } from "vitest"

describe("firestore rules", () => {
  it.todo("user can read own profile")
  it.todo("user cannot update own balance directly")
  it.todo("user cannot create wallet transaction directly")
  it.todo("regular user cannot write match result")
  it.todo("admin can read audit logs")
})
