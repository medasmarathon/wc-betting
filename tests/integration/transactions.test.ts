import { describe, it } from "vitest"

describe("Firestore transactions", () => {
  it.todo("places a valid bet before kickoff")
  it.todo("blocks duplicate bet placement")
  it.todo("settles a match idempotently")
  it.todo("voids a match idempotently")
})
